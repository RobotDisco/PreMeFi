/**
 * @constructor
 * @param title
 *            Story title
 * @param story
 *            Story body
 * @param url
 *            Story URL
 * @returns {MetaStory} Story object
 */
function MetaStory(title, story, url) {
	this.title = title;
	this.story = story;
	this.url = url;
};